services:
  backend:
    build: ./backend
    restart: unless-stopped
    volumes:
      - db-data:/app/data

  frontend:
    build: ./frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    depends_on:
      - backend

volumes:
  db-data:
